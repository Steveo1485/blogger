.post-content
  %h1= post.title
  %h5== Posted on: #{post.activated_at.strftime("%B %e, %Y")}
  = simple_format(truncate(post.content, length: 800))
  = link_to "Read More", post_path(post.id), class: "btn btn-default"

  %hr